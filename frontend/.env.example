# Leave empty in development: Vite proxies /query to VITE_DEV_API_PROXY_TARGET.
# Set a full backend URL for deployed/cross-origin frontend; backend CORS must allow it.
VITE_API_BASE_URL=
# Default: http://localhost:8000
VITE_DEV_API_PROXY_TARGET=
